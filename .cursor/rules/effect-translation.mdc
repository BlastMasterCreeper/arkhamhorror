---
description: 规则手续译 seq.*；卡牌正文译效果组合；仅保留来源归属
alwaysApply: true
---

# 效果翻译原则

## 核心（已裁决 2026-09-21）

两条轨道，不要混成「一切都是 seq」：

| 来源 | 译法 |
|------|------|
| **规则书手续**（Grimoire 章节流程、框架步、行动内核、检定、入手/显现入口、共享 Cancel/Instead） | **命名流程（`seq.*`）** + nest |
| **卡牌正文**（能力体、显现体、打出后效果体、lasting 写入） | **效果组合（Composition）** |

- 不为效果保留自然语言运行时分支（禁止 `match effect_text`、语义 atom id）。
- 与规则书原文的唯一联系：**provenance**（`AbilityUnitRef`）。
- **禁止**：Grimoire/卡面段落 → 专属 `*Policy` / `*Service`；为每张卡登记 `seq.card…`；与 Catalog 平行的第二套 dispatch。

## 统一翻译模型

```text
规则手续  →  seq.*（SequenceCatalog）
              ├─ handler 内顺序 RESOLVE（砖块：L0 / Register / 内联）
              ├─ catalog.nest / nest_batch → 另一条 seq.*
              └─ EFFECT 砖：composition.execute（跑卡面树，或本步短树）

卡牌正文  →  Composition 树
              ├─ Atom / Register / Seq(Then) / Simultaneous / If / Choice
              └─ 正文调用已有手续时 nest 那条 seq.*（抽牌、检定、Cancel…）
```

| 来源 | 译法 | 例子 |
|------|------|------|
| Grimoire 章节流程 | `seq.*` | `seq.draw.investigator`、`seq.draw.encounter` |
| 行动 | `seq.action.*` nest 内核 seq | action draw → `seq.draw.investigator` |
| 卡牌能力 / 显现体 / lasting | **Composition** | 12160：`Seq(place_doom → if_else(…, grant_surge))` |
| 卡面调用已有手续 | Composition 节点 **nest** 已有 seq | 「抽 1 张」→ nest `seq.draw.*`；Cancel → nest `seq.interrupt.cancel` |
| 子流程复用 | `catalog.nest(other_seq)` | 同一内核多入口 |

**入口不同 ≠ 新机制**：耗 action、Forced、遭遇 E4 都是 **触发/nest 同一条或相关的 seq.***。卡面本身仍是 Composition。

**打出** 不是能力、也不是卡面 seq：`PLAY_CARD` Initiation；效果体仍是 Composition。

## 命名流程 vs 效果组合

**命名流程（`seq.*`）** = 规则书里那套 **有名字、可复用、带时点** 的手续（Catalog + 堆栈 + Timing）。管何时开火、砖块顺序、Would/When/After、nest。

**效果组合（Composition）** = 卡面「这段怎么做」的可执行树。不负责开放时点；在某条 seq 的 RESOLVE 砖、或 Initiation resolve、或 LISTENER 开火时 `execute`。

```text
seq.draw.encounter             ← 命名流程（遭遇抽牌手续）
  G3 nest seq.encounter.revelation
    composition.execute(...)   ← 这张阴谋的显现正文（卡面树）

InitiationIntent.PLAY_CARD     ← 打出（独立形式）
    composition.execute(...)   ← 事件/支援打出后的卡面树

AbilitySpec
  hook: (seq.draw.investigator, WHEN)   ← 订哪条手续的哪个槽
  effect: Composition                   ← 卡面正文
```

长规范流程 = **seq handler 顺序步骤 + nest 其它 seq**。不要为卡面短句另开 `seq.card…`。

## 什么是「效果」（已裁决）

**效果（effect）** = resolve 时可 **CREATED**、对局面/规则有 **直接影响** 的写入：

| 是效果 | 不是效果 |
|---|---|
| **状态原语（state primitive）** L0 Atom → `GameStateStore`（**三类**；③ 含 Reveal） | 纯桌谈约定（不写入 Domain） |
| **Register** → `RegistrationStore` / Buff | |

见 [07-effect-primitives §0/§5.3](docs/design/07-effect-primitives.md)、[07-composition §4.1](docs/design/07-composition.md)。

## 落地要素

1. **状态原语** — L0 `StateMutator` **三类**写入（含 **`reveal_to_*`**、SetFlag/SetRef）  
2. **Register** — `RegistrationStore` / Buff（**是效果**）；查询见 [06 §16](docs/design/06-registration-buff-model.md)  
8. **`PlayerInteractionGate`** — 窗口内决策（[16](docs/design/16-player-interaction.md)；**晚于** Eligibility/ResponseWindow，见 [17](docs/design/17-seq-runtime.md) §6）  
9. **`SequenceCatalog` + handler** — 每条规则手续 `seq.*`  
10. **`ResolutionSequenceStack` + Timing** — push/pop、Would/When/After（15 D/E/ST）  
11. **`RulesMemory`** — 步间 referents  
12. **Initiation / Eligibility / TimingBus** — 窗口与能力  
13. **`AbilityUnitRef`** — provenance  
14. **`CompositionExecutor`** — 卡面树；L7 dry-run  

## 来源归属

| 字段 | 含义 |
|------|------|
| `flow_id` | 当前所在命名流程 id（卡面挂在哪条手续里跑） |
| `definition_id` | 卡牌定义 id |
| `ability_id` | 能力/效果单元 id |
| `instance_id` | 运行时实例 |

## 显现（Revelation）

- **入口 seq**（规则手续）：遭遇 `seq.encounter.revelation`；玩家入手 `seq.enter_hand`（不区分 draw/add 来源）。
- **卡面体**：该 ability 的 **Composition**；在入口 seq 的 RESOLVE 里 execute。显现后 LIMBO 弃置等仍在 **入口命名流程** 内 RESOLVE。
- 顺序占位：`EnterHandTimingPolicy`；跨类优先级见 06 §8。

## 实现约定

- **内联 vs nest**（[15 §4.0.5](docs/design/15-timing-entry-catalog.md)）：**上一步是否触发本步时点**？是 → nest 另一条 seq；否 → 内联。Then 是 Composition 内联 Seq，不是 nest。
- 新规则手续 → **`register_run` / `nest`**，不新建语义 Service。
- **Cancel / Ignore** → 卡面 Composition 的 Interrupt 节点 nest **`seq.interrupt.cancel` / `seq.interrupt.ignore`** + `InterruptTarget`（[07 §6.0](docs/design/07-effect-resolution.md)）；禁止单卡 `*CancelPolicy`。
- **Instead / Would** → Composition 的 Replace 节点 nest **`seq.replace.instead`** + `ReplacementTarget` + `replacement` 载荷（[07 §7.0](docs/design/07-effect-resolution.md)）；禁止单卡 `*InsteadPolicy`。
- **Cancel encounter revelation** → 仅注销 G3 显现；**G4 仍 discard** 至 encounter discard（FAQ · [07 §6.0.1](docs/design/07-effect-resolution.md)）。
- **Cannot** → Register RESTRICTION + [06 §16](docs/design/06-registration-buff-model.md) Intent 入口。
- 薄 facade 仅 `catalog.run(flow_id, params)`。
- 冲突：07 §3.2–§3.4（Cannot / 竞争 / tier / Silver Rule）。

## ArkhamDB 编译（`tools/arkhamdb_abilities.py`）

- 卡面 effect 段编译为 **Composition 树**（template / Atom / Register / If / Choice / nest 已有 seq）。
- **优先复用**已有 template / condition / `if_kind` / `evaluate` 锚点（07 §3.3、15 §17、已竖切卡如 12126/12160）。
- **能拆就拆**：timing · condition · cost · effect 分编译单元；禁止无先例的新语义 atom。
- **无可参考先例时**：先查设计文档与 imported JSON 同类卡；仍无法归类再询问是否扩展编译器。
